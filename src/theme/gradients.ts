import * as c from './colors';

export const brandGradient = `linear-gradient(135deg, ${c.primaryGradientStart}, ${c.primaryGradientEnd})`;
export const brandGradientVertical = `linear-gradient(to bottom, ${c.primaryGradientStart}, ${c.primaryGradientEnd})`;

export const playerGradient = `linear-gradient(to bottom, ${c.playerDark1}, ${c.playerDark2}, ${c.playerDark3})`;

export const miniPlayerGradient = `linear-gradient(to right, ${c.miniPlayerStart}, ${c.miniPlayerMid}, ${c.miniPlayerEnd})`;

export const welcomeGradient = `linear-gradient(to bottom, ${c.primaryGradientStart}, ${c.primaryGradientEnd}, transparent)`;

export const playButtonGradient = `linear-gradient(135deg, ${c.playerAccent}, #D4960A)`;

export const loginGradient = `linear-gradient(135deg, ${c.playerAccent}, #D4960A, #1A2744)`;
